import React from 'react';
import { Bounce, Pin, Pulse } from './Marker.styles';

export default function Marker () {
    //const { color, name, id } = props;
    return (
        
       <div>
            <Pin><Bounce>   </Bounce></Pin>
            <Pulse />
       </div>
    );
} 